
import React from "react";

const Painel = () => (
  <div style={{ padding: "40px" }}>
    <h1>📊 Painel Administrativo</h1>
    <p>Gráficos, filtros, exportações, Firebase Firestore.</p>
  </div>
);

export default Painel;
